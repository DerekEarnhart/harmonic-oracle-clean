import { AppShell } from "@/components/AppShell";
import ARCMetaLearner from "@/components/labs/ARCMetaLearner";

export default function ArcMetaLearner() {
  return (
    <AppShell title="ARC Meta-Learner" subtitle="HCS-inspired meta-learning lab UI">
      <ARCMetaLearner />
    </AppShell>
  );
}
